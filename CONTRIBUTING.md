# 기여 규칙

이 저장소는 `zdp-architecture`의 정책 원천을 실행 가능한 검증기로 옮기는 내부 도구 저장소다.

## 변경 원칙

- 새 정책은 이 저장소에서 임의로 만들지 않는다. 먼저 `zdp-architecture`의 문서, 카탈로그, 스키마, 규칙, fixture 중 하나에 근거가 있어야 한다.
- 규칙은 고칠 파일, 고칠 필드, 규칙 ID가 분명한 진단으로 구현한다.
- 저장소 이름 목록을 코드에 직접 박지 않는다. 가능한 한 `catalogs/repositories.yaml`, `catalogs/services.yaml`, `service.yaml`에서 판단한다.
- 돈, 권한, 개인정보, AI 사용자 데이터, 인증 정보, 감사 경계는 보수적으로 차단한다.

## 코드 구조

- 카탈로그 로딩, 그래프 구성, 규칙 평가, CLI 출력, 테스트 fixture를 분리한다.
- 새 규칙은 기존 규칙 파일의 책임과 맞으면 그 파일에 추가하고, 책임이 새로 생기면 작은 모듈로 분리한다.
- 진단 메시지는 사람이 바로 고칠 수 있게 작성한다.

## 테스트 규칙

- 새 차단 규칙에는 최소 하나의 통과 사례와 하나의 실패 사례를 둔다.
- `--repository` 동작이 바뀌면 실제 저장소 검증도 함께 확인한다.
- 테스트 기대값은 규칙 ID, 파일, 경로, 메시지를 안정적으로 검증한다.

## 릴리스 규칙

- 검증 동작이나 CLI 출력이 바뀌면 `package.json` 버전을 올린다.
- 변경 내역은 `CHANGELOG.md`에 남긴다.
- 커밋 전 `bun run check`, `bun test`, 실제 `zdp-architecture` 검증을 통과시킨다.
